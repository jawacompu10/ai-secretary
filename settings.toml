# Personal Secretary Configuration

[default]
user_name = "Jawahar"
# Calendar settings
calendar_url = "http://localhost:5232"
calendar_username = "admin"
calendar_password = "admin"

# OpenAI settings (optional - for AI agent functionality)
openai_api_key = ""
model_name = "gpt-4o-mini"

# Application settings
check_interval = 300  # 5 minutes in seconds
debug = false

# Task settings
default_task_duration = 30  # minutes
max_tasks_per_day = 20

[development]
debug = true
check_interval = 60  # 1 minute for faster testing
model_name = "gpt-4o-mini"

[production]
debug = false
check_interval = 300  # 5 minutes
model_name = "gpt-4o"
